# Verzek AutoTrader Backend Configuration

# Server
FLASK_ENV=production
PORT=8050
SERVER_IP=80.240.29.142

# Security (REQUIRED)
JWT_SECRET=VerzekAutoTraderKey2025
API_KEY=Verzek2025AutoTrader

# Encryption Key (REQUIRED - Generate with: python3 -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())')
ENCRYPTION_KEY=<GENERATE_ME>

# Database (SQLite default, Postgres ready)
DATABASE_URL=sqlite:////root/api_server/database/verzek.db

# Exchange Mode
EXCHANGE_MODE=paper
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Telegram Broadcasting (REQUIRED for broadcasting)
TELEGRAM_BOT_TOKEN=<YOUR_BOT_TOKEN>
TELEGRAM_VIP_CHAT_ID=<YOUR_VIP_CHAT_ID>
TELEGRAM_TRIAL_CHAT_ID=<YOUR_TRIAL_CHAT_ID>

# Worker Configuration
WORKER_POLL_SECONDS=10

# Email (optional for future)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
